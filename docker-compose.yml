services:
  next-app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: matts-gardens

    ports:
      - "3001:3000"

    restart: unless-stopped

    environment:
      NODE_ENV: production